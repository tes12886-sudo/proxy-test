[project]
name = "mock-server"
version = "0.1.0"
description = "Mock Server Login"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "pycryptodome>=3.20.0",
    "blackboxprotobuf>=1.0.1",
]

[tool.vercel]
entrypoint = "api.MajorLogin:handler"
